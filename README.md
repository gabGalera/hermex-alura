# Hermex

Frontend do Hermex (Vite + React + TypeScript).

## Scripts

```bash
npm install
npm run dev
npm run build
npm run lint
```

## Estrutura

Código em `src/` organizado por responsabilidade: `pages`, `components`, `hooks`, `services`, `stores`, `styles`, `utils`, `types`, `contexts`.
